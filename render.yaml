services:
  - type: cron
    name: keiba-pipeline-test
    env: python
    # ★ 修正点: エラーに従い、cronサービスのplanキーを削除します
    region: oregon
    schedule: "0 20 * * *"
    startCommand: "echo 'Test successful: YAML parsed correctly!'"