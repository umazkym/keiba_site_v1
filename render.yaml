services:
  - type: cron
    name: keiba-pipeline-test # 念のため名前を変更
    env: python
    plan: free
    region: oregon
    schedule: "0 20 * * *"
    startCommand: "echo 'Test successful: YAML parsed correctly!'"