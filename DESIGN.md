# UMA-FREE Design System

## 目的

UMA-FREEの画面は「ロゴの丸い線と同じ手触りの、競馬場で素早く読めるデータ紙面」を目指します。入口（ホーム・重賞・記事・SNS）は写真と大きな見出しで気持ちを上げ、中身（出走表・比較・データ）は白い紙面で数字を読みやすくします。AI生成物を想起させる均一なカード配置や意味のない演出は避けます。

### 2026-09-24 デザイン改修で決めたこと（ポートフォリオ承認済み）

ポートフォリオ：https://claude.ai/artifact/YBhUXwCjgzfpLKTgXxyjoQ（生成元は git 対象外の `.local/design-portfolio/src/`。`lib.mjs` が色・文字・部品の見本の正本）

- 方向性は A×C。ホーム・重賞・記事の入口は写真（C）、その下は白い紙面のセクション（A）。写真は入口だけに使い、出走表・数値の比較には入れない。
- ロゴは同じ図柄を清書したSVG（`frontend/public/brand/`、React は `components/BrandLogo.tsx`）。32px以下は内側の線を省いた小さい版を使う。
- ダークモードは作らない。色は変数にしておく。
- 案内役（ロゴの馬）は空の状態・404・ガイドだけに控えめに使い、分析画面には出さない。
- 的中の数え方と表示は変えない（回収率は出さない）。
- 実装は段階0（ロゴ・色・文字）→ 1 共通の枠 → 2 レース画面 → 3 ホーム → 4 SNSの画像と文面 → 5 記事・データ → 6 動画の順に進め、段階ごとに確認をもらう。進捗は `.claude/CLAUDE.md` セクション8に記録する。
- 写真素材は `reference/UIdesign/uma-free-assets/` に置く（寸法・構図は `.local/design-portfolio/ASSET_SPEC.md`）。届くまでの枠は灰色一色の予約面にする。

### 2026-09-21 ユーザーフィードバック（引き続き有効）

情報密度だけを優先した無機質な案は不採用。競馬らしさ、見たい・操作したいと感じる表現、画像とイラストを情報への入口に使うことを必須の判断軸とする。スマホを先に構成し、画像の構図・切取り・配置をPCと分ける。主要なレースへのリンクは画像を眺めるだけで終わらない位置へ置く。下記の寸法は既存仕様の記録であり、異なる案は可読性・比較量・独立した操作領域・スクロール・実画面で採否を判断し、理由を記録する。

この文書はUI判断の単一の基準です。新しい画面やコンポーネントを追加するときは、既存実装、`AGENTS.md`、関連SOPとあわせて参照してください。

## 情報設計

1. 画面ごとに主役を一つ決める。
   - ホーム: 今日のレース分析への入口
   - 開催日: その日の全会場×全レースの一覧
   - レース: 出走馬のAI偏差値比較
   - 記事: 見出しと本文
   - データハブ: 調べる対象の選択と横断検索
   - データ詳細: 母数を伴う条件別成績
   - 比較: 選択した馬の同じ指標による横並び
2. 「最初に見る → 比較する → 行動する」の順に並べる。
3. 実データの0件、欠損、長い馬名、18頭立て、長い記事タイトルを通常状態として設計する。
4. 同じ重要度のカードを並べて主役を曖昧にしない。カードは情報の独立性が必要な場合だけ使う。

## ロゴ

- マーク：インディゴの円に、紺の線で描いた白い馬の横顔。`BrandMark`（`size` と `variant`）。
- 横組み：マーク＋「UMA-FREE」（M PLUS Rounded 1c 800、マークの0.56倍）。`BrandLockup`。タグラインは「完全無料のAI競馬分析」。
- 32px以下（favicon・スマホのヘッダー）は小さい版（輪郭を太く、内側の線なし）。
- 暗い面・写真の上では文字を白にし、マークはそのまま使う。マークの色を変えない、縁取り・影・回転をしない。
- ファイル：`public/brand/uma-free-mark.svg`・`-small.svg`・`-mono.svg`、`icon-192/512.png`、`icon-maskable-512.png`、`apple-touch-icon.png`、既定のOG画像 `og-default.png`（1200×630）。`public/new-logo.png` は構造化データの logo URL のため名前を残し、中身を透過の新ロゴにしてある。

## 色

色は役割で使い分け、同じ色に複数の意味を持たせません。値は `tailwind.config.ts`・`app/globals.css`・`lib/brand.ts`・`backend/scripts/brand_tokens.py` の4か所で同じにし、`npm run design:audit` が一致を確かめます。

| 役割 | 色 | Tailwind | 用途 |
| --- | --- | --- | --- |
| ブランド・操作 | インディゴ `#4C4EFF` | `brand-600`（文字は `brand-700` `#3638D6`） | リンク、選択中、主ボタン、フォーカス |
| 構造 | 紺 `#1C2787` | `navy` | 見出しの識別線、会場×Rのプレート |
| 夜の面 | `#0E1440` | `night` / `slate-950` | フッター、写真の上の幕、SNS・動画の暗い面 |
| 文字 | `#151A3D` / `#3A4063` / `#5A6183` | `slate-900` / `slate-700` / `slate-500` | 本文・補足・注記 |
| AI偏差値 | 琥珀 `#F2A516`（数字は `#865300`） | `ai` / `ai-deep` / `ai-soft` | AI偏差値の棒、◎、上位の強調（面と棒に使い、白地の文字には ai-deep） |
| コース | 芝 `#2E8B57`・ダート `#A5692F`・障害 `#6C54C8` | `turf` / `dirt` / `jump` | コース図、コース種別の表示 |
| グレード | G1 `#1F5FD1`・G2 `#D23B3B`・G3 `#1E8E4F`・地方 `#8E5E26` | `grade-g1` など | グレードのバッジだけ |
| 良好状態 | Emerald `#059669` | `emerald-*` | 更新済み、正方向の状態 |
| エラー | Red `#dc2626` | `red-*` | 取得失敗、入力エラー |
| 楽天競馬PR | Rose `#e11d48` | `rose-*` | 楽天競馬のPR導線だけ |

- `slate` はロゴの紺に寄せた青みのある灰に置き換えてある（`slate-50` `#F3F5FA` が背景）。`gray-*` は使わず `slate-*` を使う。
- Tailwind の `blue-*` は使わない（監査で0件）。ブランドの青は `brand-*`、4枠の青は `waku-4`。
- 枠色は競馬固有の識別に限って使い、定義は `lib/waku.ts` の `getWakuClasses()` の1か所だけにする。
- 本文色は原則 `slate-700` 以上、意味のある小さな文字でも `slate-500` 以上。通常本文のコントラストは4.5:1以上を維持する。

## タイポグラフィ

- 見出しと数字の書体は `app/layout.tsx` の next/font で読み込む（自前配信、外部CSSなし）。
  - 見出し・ロゴ文字：M PLUS Rounded 1c 800（`font-display`、h1・h2 の既定）。ロゴの丸みに合わせる。
  - 数字（AI偏差値・オッズ・距離・配当）：Barlow Semi Condensed 500〜700（`font-num`。`font-mono` も同じ書体を指す）。桁をそろえる箇所では `tabular-nums` を併用する。
  - 本文：端末の日本語書体（`font-sans` ＝ `--font-body`：ヒラギノ角ゴ → Noto Sans JP → 游ゴシック → メイリオ）。
- 日本語の書体は文字範囲ごとの分割ファイルになり、1ウェイトごとに描画を止めるCSS（@font-face の宣言）が gzip で約31KB増える。本文に Noto Sans JP を配信したり、見出し書体のウェイトを足したりしない（2026-09-24 にビルドで計測。本文＋見出し2ウェイトで約93KB増えたため、本文は端末の書体、見出しは800だけにした）。
- 本文は15〜16pxを基準とする。スマホも本文14〜15px、補足12px以上を基準にし、11px未満は出走表など比較表の中だけにする（監査の `tiny-text` で数を増やさない。段階1〜5で減らし、減らしたら上限も下げる）。
- スマホの見出しの上限は、ページ見出し(h1) 24px・セクション見出し(h2) 20px・小見出し(h3) 18px。大きさは各画面のクラスで決め、`globals.css` で h1〜h4 や p を一律に `!important` で押さえ込まない（2026-09-24 に「見出し上限15px」の一括指定を廃止した。監査 `no-global-mobile-heading-cap`）。レース画面と記事の専用規格はそれぞれのscopeで持つ。
- 640px未満の記事詳細は、これまで本文12px・H2 16px・H3 14pxの専用規格だった。段階5で本文15px前後・H2 20px前後へ引き上げる（ユーザー承認済み。適用までは監査の `mobile-article-readable-type` が旧規格を守る）。
- ページ幅はホーム・レース1280px、記事一覧・データ1152px、記事紙面1080pxを上限とする。
- 記事本文はPCで1080pxの紙面内に置き、通常の見出し・段落・リストは760pxを上限とする。表・図・画像だけは1080pxまで広げ、各要素の左端を揃える。モバイルは画面端から8pxの余白を基準とし、表だけ横スクロールを許可する。
- 見出しは太さと余白で階層を作る。小さな英大文字ラベルを繰り返して階層の代わりにしない。

## 写真とイラスト

- 写真は入口だけ：ホームのファーストビュー、重賞の見出し、記事の冒頭、SNS。出走表・データ表・比較には使わない。
- 写真の上に文字を置くときは、サイト側で夜の紺（`#0E1440`）の幕を重ね、白い文字と白いカードを載せる。写真そのものに文字を焼き込まない。
- 実在の馬・騎手と特定できる写真（勝負服・ゼッケン）は、名前との対応を確かめられない限り使わない。
- 素材が無い枠は灰色一色の予約面にし、高さを確保してレイアウトを動かさない。
- コース図はロゴと同じ丸い線で描いた24場の図（芝＝緑、ダート＝茶）。案内役の馬は空の状態・404・ガイドだけ。

## 言葉

- 絵文字を使わない。投稿文・画面ともに事実を短く書く。
- 行動を促す言葉は「確認する」「判断材料」。「必勝」「絶対」「推奨」「鉄板」は使わない。
- 使っていないデータを名前に含めない（例：オッズを使わない機能に「オッズ妙味」と付けない）。

## 共通の部品

- ボタンは `globals.css` の `ui-btn` の1系統だけを使う。高さ44px（`ui-btn--l` は52px）、角丸11px、文字15px太字。
  - `ui-btn--primary`：インディゴ。1画面に1つの主な操作。
  - `ui-btn--secondary`：白地に枠。主ボタンの横の別の選択肢。
  - `ui-btn--navy`：紺。写真や強い区切りの中の主ボタン。
  - `ui-btn--white`：白。紺の面や写真の上。
  - `ui-btn--ghost`：文字だけ。補助の操作。
  - 旧来の `.btn-primary`・`.recent-return-primary`・`.cta` は見た目を合わせてあり、使っている画面の段階で `ui-btn` に置き換える。Tailwind で直接 `bg-slate-950` や `bg-primary` のボタンを作らない。
- アイコンは `components/LineIcon.tsx`（ロゴの線に合わせた丸い線、24px基準で線幅2）。新しい形はポートフォリオの `lib.mjs` に足してから書き出す。
- 案内役の馬は `GuideHorse`（`components/BrandLogo.tsx`）。表情は normal（案内）・sleep（開催なし）・look（探している・エラー）・lost（404）。
- グレードのバッジは `grade-badge grade-badge--g1|g2|g3|local`。
- フッターは夜の紺（`bg-night`）に横組みロゴ（白）、分析／データと記事／このサイトについての3列（スマホは2列のリンク、1行40px以上）、最下段に「投票の推奨ではありません。馬券の購入は20歳になってから。」を置く。
- モバイルメニューはアイコン付きの52pxの行（本日のレース分析・データベース・記事・AI予想の成績・よくある質問・検索）、その下に運営情報のリンクと注記。

## 面・境界・形

- 通常の角丸は8〜12px。ピル形状はタグ、件数、状態表示に限定する。
- 枠線と広い影を同時に使わない。
- 通常コンテンツは1pxの境界線または背景差で分ける。影はsticky要素、メニュー、オーバーレイに限定する。
- モバイルで外側セクションに境界がある場合、内部の反復項目はカードを重ねず区切り線へ落とす。カード内カードによる二重の枠線を避ける。
- 広告読込中は静止した予約面を使い、継続するpulse表示でスクロール中の再描画を増やさない。広告の予約高と実広告の操作性は維持する。
- 入れ子のカードを避け、同一セクション内は罫線や余白で関係を示す。
- ガラス表現はstickyヘッダーなど、背後の文脈を保つ必要がある面だけに使う。

## 操作とモーション

- 入力直後に色、背景、境界で反応を返す。
- 色・透明度の遷移は120〜180msを基準とする。
- hoverで要素を持ち上げる、画像を拡大する、押下時に縮小する演出は使わない。
- 継続的なpulseは読み込み状態以外で使わない。
- ページ内スクロールは`prefers-reduced-motion`が有効な場合に即時移動する。
- すべての操作要素に可視フォーカスを用意し、タッチ領域は44px以上を確保する。

## ページ別ルール

### ホーム

- 1画面目は写真の入口（`HomeHero`、2026-09-25 段階3）。中央の開催がある日は芝の写真、地方だけの日は16時まで昼のダート・16時以降はナイターの写真。写真は事前に書き出したWebP（PC 1280/1920・スマホ 720/1080）を `<picture>` で出し、`fetchPriority="high"`。紺の幕（`.home-hero-scrim`）はスマホ〜タブレットで上から下、PC（1024px以上）で左から右へ暗くする。
- 見出しは「園田・浦和・笠松／全35レースの分析を公開中」（5場以上は「中央3場・地方4場」と数える）。開催がない日は「今日のレース分析を／無料で確認できます」。説明は1文、CTAは「今日の全レースを確認する」の1つだけ（`hero_cta`）。
- スマホ・タブレットは写真の下端に白いカードを重ね、4つの視点（`RaceAnalysisValueGrid` の `full`）とCTAを置く。PCは写真の右上に今週の重賞のカード（`HomeGradeMini`）、写真の下端に4つの視点の帯（`bar`）を重ねる。PCの見出し行の右に「前回の続き」（見たレースが無ければ出さない）。
- 本日の開催は会場カード（会場名・全NR・芝ダ・コース図・メインまたは最終レース・グレード・AI 1位）。PCの列数は会場数に合わせ（3場なら3列）、空の列を作らない。スマホ・タブレットは「AI 1位 … 偏差値」と馬名を2段に分ける。見出しの右に開催日ボードへのリンク（`board_link`）。
- 今週の重賞はスマホ・タブレットで写真つきのカード（`WeeklyGradeRaces` の `feature`）、PCは写真の上のカードと右列の「そのほかの重賞」。重賞が無い日は同じ位置に「今日のレース分析を確認する」（`grade_fallback`）。
- 本日の分析注目馬は「本命候補・対抗以下の上位・地方の注目」。オッズは使っていないため、オッズを根拠にした呼び方（妙味・割安）はしない。注目馬が無い日はセクションごと出さない。
- 高配当的中ランキングは数え方・表示する項目を変えない（見た目だけ）。
- 記事はスマホで1列のリスト（4件）、PCで3列のカード。汎用のアイキャッチはカテゴリの写真に置き換え、同じ一覧で同じ写真を並べない（写真が尽きたらカテゴリ色の面＋線のアイコン。`lib/article-visual.ts`）。
- 見出しはホームだけ下線と色の棒を付けず、丸い書体で19〜21px（`.home-page-scope .ui-section-header`）。各セクションは白い紙面（角丸12px・1pxの線）。
- PCの右列は広告・そのほかの重賞・データベースの検索・記事のカテゴリ（件数つき）。
- モバイルで横に隠れるレールを作らない。
- 広告の位置と計測名は変えない：本日の開催の後（`home_after_today_races`）、注目馬の後（`home_after_today_pick`）、記事一覧の後（`home_article_feed_1`）、PC右列の先頭（`home_after_special_pick`）。PR枠（`home_nar_voting`）は会場カードの下。
- 「今日の全レースを確認する」の固定CTAは全画面幅で画面下端へ常時固定し、主CTAの通過、フッター接近、下部アンカー広告の開閉には追従させない。広告が前面に出た場合は広告側の重なりを優先し、サイトCTAを上下移動させない。

### 開催日ボード（/races/{日付}）

- その日の全会場×全レースを1画面で見せる一覧（2026-09-24 段階2で新設）。見出しは「9月20日(日)のレース分析」、その下に「中央2場・地方2場 · 45レース · 結果の反映状況」と日付送り（前日・日付・翌日・今日）。
- 中央／地方はタブ（開催のない側は「開催なし」で押せない）。PCは会場ごとの列（最大4列で折り返す）、スマホは会場ボタン（コース図つき）で1会場を選び、そのレースを1列で並べる。
- 各行は R番号の四角・レース名・グレード・芝ダ距離・頭数・AI 1位（馬番・馬名・偏差値）。結果が出たら1着の馬と、その馬の印・AI順位を足す。AI偏差値の対象外のレースは理由を書く。行全体がレース詳細へのリンク（`prefetch={false}`）。
- 全馬の予測データはクライアントへ送らず、サーバーで一覧に必要な値だけに要約して渡す（`lib/race-day-summary.ts`）。コース図もサーバーで描いて渡す。
- 中央・地方の切り替えは `race_group_select`、スマホの会場ボタンは `race_venue_select` を送る（名前・パラメータは従来どおり）。ボードでは `race_view` を送らない。
- ボードの下に、前回見たレース・注目馬・重賞・免責・的中・PR・記事内広告（実験）を従来の日付ページと同じ順で置く。

### レース

- 見出し（`RaceHead`、サーバーで描く）：会場×Rのプレート（紺）・レース名（h1。スマホ22px・PC34px、2行まで）・グレードのバッジ・日付・芝ダ距離・頭数・馬場と天候（あるときだけ）・コース図。重賞は季節の写真を入口にする（スマホは上の帯60px、PCは右側に白へ溶ける写真）。結果が出たら「結果確定 1着 馬名 AI偏差値◯（◯頭中◯位）」を並べる（数え方は変えない）。
- 390x844の初期表示で、AI偏差値表の先頭4頭を確認しやすい密度を基準とする。
- スマホの文字は、セクション見出し17px・本文13px・出走表の馬名15px・AI偏差値の数字18px。`race-page-scope` で見出しを一律に小さくする `!important` は使わない（監査 `mobile-race-readable-type`）。余白を持つカードの見出しには `race-section-heading--flush` を付ける。
- 開催・レース選択と予想表の内部に広告やPRを置かない。
- 出走表の列：印（◎は琥珀の文字、○はインディゴ、▲は紺）・馬番（枠色の丸）・馬名（スマホは下に位置取りの3点）・AI偏差値（数字＋横棒。◎の行だけ琥珀と淡い琥珀の行）・位置取り（PC）・着（結果後）。並べ替えは AI偏差値順・馬番順・着順（結果後）。欠損は「—」と「データ不足」。AI偏差値の対象外のレースは理由を上に1回だけ書き、各行は「—」にする。
- 位置取り（先行・中団・後方）はレース内の相対値で分ける（`getPositionLabels`）。出走表と AIレース展望で同じ判定を使う。
- 馬番の傾向はコース全体のデータから今回の出走馬の馬番だけを並べ、「枠」ではなく「馬番」と書く。最も良い馬番はインディゴ、最も悪い馬番はローズ。
- 展開予測は芝の淡い面に馬番を置き、AI偏差値の上位3頭に琥珀の輪。右ほど前（「先行・逃げ →」）。
- レース内ナビは「AI偏差値・対戦比較・展開・脚質・馬番の傾向」の4項目に固定し、線アイコン（gauge・swords・lanes・bars）を使う。AI展望の本文は残すが、主要4視点のナビへ混在させない。
- モバイルの固定ボトムナビは4列。選択中は紺の文字・インディゴのアイコン・上端のインディゴの線（3px）。各項目は44px以上、ナビ全体はsafe-areaを含めて本文へ重ならない予約余白を確保する。
- レース選択は現在の競馬場・レース要約（紺の「中山 11R」の札とレース名、スマホは開催日ボードへの「全レース」）と1〜12Rを一つのsticky面にまとめる。選択中のRは紺。PCは1行、モバイルも全番号を1行に収めて横スクロールさせず、ヘッダーとの間に8px以上の余白を保つ。固定親を一つに限定し、子要素へ二重の`sticky`を付けない。
- レース選択のsticky面はヘッダーを除いて88px以内に収め、予想表側に同じレース要約を重複表示しない。
- デスクトップの右サイドバーは同日レースを重複表示せず、「分析メニュー」として4分析ナビを縦に配置する（選択中は淡いインディゴの面）。
- 展開予測はPCとモバイルで同じ位置取りグラフを使う。モバイルは馬名ラベルを省略してグラフ本体を128px程度へ圧縮するが、後方・中団・先行の方向と馬番配置は維持する。馬番の傾向と表示領域高を揃える。
- 4分析ボトムナビはAI偏差値表が画面へ入ってから関連記事までのレース分析範囲で表示する。Google下部アンカーが重なる時は広告DOMを変更せず、サイト側ナビを一時退避する。
- 次のレースは R番号の四角・レース名（詳細ページでは「中山12Rの分析を見る」）・AI 1位のカードにする。

### 記事

- 記事冒頭はH1 → リード → サイト価値ガイド → アイキャッチ → 目次 → 本文の順にする。カテゴリ・公開日・読了時間・著者は本文理解を遅らせるため、タイトル上へ重複表示しない。
- サイト価値ガイドはホームと同じ4つのミニグラフを使い、当日の全レース分析へのCTAを一つだけ置く。ホームは情報量のある`full`、記事冒頭は`compact`を使い分ける。
- 記事用ガイドは「今日の全レースを4つの視点で確認」と「AI偏差値・対戦比較・展開／脚質・枠順傾向」だけを表示し、説明文や独立した大型CTAを置かない。
- 記事用ガイドはモバイルでも4列を維持し、パネル全体を単一リンクにする。390px幅で全高約130px以内、各操作領域44px以上、明瞭なfocus表示を保つ。
- 記事テーマはモバイルで「重賞・騎手・コース」の3分割バーにまとめ、初期状態は44pxのバーだけを表示する。選択中テーマを押したときだけ内容を直下へ表示し、カテゴリ一覧は横スクロールさせず折り返す。閉じたバーはスクロール中もヘッダー表示高の直下へ追従する。
- 重賞テーマは二段階のアコーディオンとし、最初にG1・G2・G3・その他・Jpn1・Jpn2・Jpn3・その他だけをこの順で表示する。各重賞名は該当グレードを開いた中にだけ表示する。
- コーステーマも二段階とし、最初に競馬場名だけを表示し、その中に各距離・馬場のコースを置く。重賞・コースの階層面はSlateの単色系で統一し、グレードごとの多色塗り分けを使わない。
- 記事詳細のタイトル上には、長い記事名が省略記号だけになる前後ナビを置かない。回遊は本文後の「次に読む分析」とテーマ一覧へ集約する。
- 共通アイキャッチはOG画像と関連記事の代替表現に限定し、本文では表示しない。関連記事の代替表現にはカテゴリ別アイコンを使う。
- 目次とレース導線は紙面の補助情報として平坦に扱い、グラデーションや広い影を使わない。
- 重賞記事の対応レース導線は、開催日・レースID・正確なURL・AI偏差値データを事前検証できた場合だけ、リード直後へ表示する。上位3頭はAPI実値だけを使う。
- 対応データがない重賞記事では、ローディング枠、空のカード、「準備中」、推測した別レース、`/races/today`への代替導線を出さない。DOM自体を描画せず余白も残さない。
- 検証後の一時API障害時だけ、保存済みのレース名・日付・競馬場と正確なレースURLを残し、予測行を隠す。別レースへフォールバックしない。
- 広告とアフィリエイトの既存位置、予約高、計測を維持する。
- Googleのオファーウォールやアンカー広告が閉じた後は、表示中のGoogle UIやサイトメニューがないことを確認してから`body/html`のスクロールロックと広告由来の上下paddingを復旧する。

### データベース

- 一覧は名前、補足、母数を一行で比較できる密度にし、個別カードを並べ過ぎない。
- 詳細は通算、コース、距離、馬場、人気帯の順に置き、勝率・連対率・3着以内率には必ず出走数を併記する。
- 母数が少ない対象は`noindex`とし、検索流入向けのページ数を増やすこと自体を目的にしない。値がない箇所は推測せず`—`を表示する。
- 条件別成績の表はモバイルで横スクロールを許可し、先頭列の見出しと率の桁を揃える。
- 条件別成績と比較表は表内だけを横スクロールさせ、先頭列を固定する。固定列には行背景を引き継ぎ、率・出走数・欠損値を同時に確認できる状態を保つ。
- 保存と比較はデータ閲覧を妨げない補助操作として見出し付近へまとめ、タッチ領域44px以上と可視フォーカスを保つ。
- マイデータと比較画面は個人のブラウザ状態を使うため`noindex`とし、保存内容をサーバーへ送信しない。
- 比較画面はPCで通算・同条件・直近5走を連続表示し、モバイルでは同じ3区分を切替表示する。選択中の2〜5頭はコンパクトなsticky要約に残す。
- 比較画面では0頭、1頭、2頭、5頭、読込中、API失敗、検索0件、少数データをそれぞれ独立した状態として表示する。
- レース画面の条件別比較は初期表示で取得せず、利用者が展開した時だけAPIを呼ぶ。既存の予想表・広告・4分析の順序を崩さない。
- コースの枠番、馬番、脚質、騎手、調教師、払戻分布は同じ期間・馬場・距離条件で集計し、集計期間を画面に明示する。

### 共通ヘッダー

- 共通ヘッダーは全画面幅でスクロール位置に関係なく上部固定し、実測高を共通スペーサーで本文前に予約する。
- ロゴはスマホでもマークと「UMA-FREE」を並べる。PCの主要ナビは現在地を紺の太字とインディゴの下線（3px）で示し、背景の塗りは使わない。
- 上部アンカー広告がある場合、折りたたみ操作部相当の最大32pxだけをヘッダー上に予約する。PCは広告DOMの遅延生成による初期移動を防ぐため、広告非表示時も32pxを固定予約する。1024px未満は検出した操作部高を使用し、いずれも広告本体の展開高には追従せず、広告がヘッダーへ重なることを許容する。
- 768px前後のタブレット幅ではモバイルメニューを維持し、主要ナビの横並びは1024px以上で表示する。PR・検索・メニューを同時に置いてもページ幅を押し広げない。
- ヘッダーに追従するsticky要素は個別の固定値を持たず、`--site-header-offset`を参照する。
- 視覚的なパンくずは回遊と現在地確認のため残し、モバイルでは「ホーム / 現在地」の1行へ圧縮する。上下6px前後の呼吸を持たせ、後続見出しへ密着させない。構造化データの`BreadcrumbList`は表示とは独立して維持する。
- モバイルメニューは`backdrop-filter`を持つヘッダーの外側へ配置し、背景を`inset: 0`、パネルを広告操作部の予約高を含むヘッダー直下から`100dvh`の残りまで固定する。
- Escape、背景タップ、フォーカストラップ、初期フォーカス、復帰フォーカスを備え、メニュー展開中は背後のスクロールと操作を止める。

## 禁止パターン

- `transition-all`
- hover時の`translate`または`scale`
- 同じ角丸・影・余白のカードを全面に敷き詰める構成
- 意味のないグラデーション、装飾目的のglass、常時アニメーション
- 押せそうに見える非操作要素
- 長文、0件、欠損値を省いた確認

## 検証

- `npx tsc --noEmit` → `npm run lint` → `npm run design:audit` → `npm run build`（同時に走らせない）
- 375px、390px、768px、1024px、1440pxで横スクロール、sticky重なり、タッチ領域を確認する。
- 長い馬名、18頭、欠損スコア、重賞0件と多数、アイキャッチなし、長い英数字タイトル、幅広い表、広告空振りを確認する。

## 参照した外部知見

- ui-ux-pro-max: 検索可能な設計知識、アクセシビリティ、実装前チェック
- Impeccable: 共通語彙、既存デザイン優先、カード・影・モーションの抑制
- Frontend Design Discipline: 失敗条件の数値化、1変更1失敗パターン、反復検証
- Apple Design skill: 即時反応、空間的一貫性、控えめな素材、動きの中断可能性
- X記事: 主役の明確化、色の役割、実データの端ケース、削る工程

外部リポジトリのスキル本体は取り込まず、UMA-FREE固有の判断だけをこの文書とSOPへ残します。
