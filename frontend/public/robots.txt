User-agent: *
Allow: /

# 静的リソースへのクロールを禁止（クロールバジェット節約）
Disallow: /_next/static/media/
Disallow: /_next/image

# クロール不要なパス
Disallow: /api/
Disallow: /node_modules/

# 検索機能のクロールを禁止（動的コンテンツ）
Disallow: /search?*

# 重複URLパターンを禁止（venue→raceの順序）
# 正規化されたURL（race→venue）のみをクロール対象とする
# ただし、Disallowでのクエリパラメータ順序指定は効果が限定的なため
# ミドルウェアでのリダイレクトと併用

# サイトマップの場所を通知
Sitemap: https://uma-free.com/sitemap.xml

# Googlebotは優先的にクロール（Crawl-delay不要）
User-agent: Googlebot
Allow: /
Disallow: /api/
Disallow: /search?*

# その他のボットにクロール遅延設定
User-agent: *
Crawl-delay: 1