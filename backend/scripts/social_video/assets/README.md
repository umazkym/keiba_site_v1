# 動画用写真素材

このディレクトリには、権利確認済みの競走馬・競馬場写真だけを配置します。
外部URLや参考サイトから保存した画像は使用しません。

最低限、次の2枚を `images/` 以下へ配置し、`manifest.json` の `defaults` に登録します。

- 横長: 1920x1080以上
- 縦長: 1080x1920以上

素材は `日付:競馬場:レース番号`、競馬場、共通画像の順で解決されます。
`focus` は写真内で残したい中心位置を `[x, y]` の0から1で指定します。
