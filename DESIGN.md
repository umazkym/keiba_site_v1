# UMA-FREE Design System

## 目的

UMA-FREEの画面は「競馬場で素早く読む、現代的なデータ紙面」を目指します。装飾よりも、最初に見る情報、比較する情報、次に行う操作の順序を優先し、AI生成物を想起させる均一なカード配置や意味のない演出を避けます。

この文書はUI判断の単一の基準です。新しい画面やコンポーネントを追加するときは、既存実装、`AGENTS.md`、関連SOPとあわせて参照してください。

## 情報設計

1. 画面ごとに主役を一つ決める。
   - ホーム: 今日のレース分析への入口
   - レース: 出走馬のAI偏差値比較
   - 記事: 見出しと本文
   - データハブ: 調べる対象の選択と横断検索
   - データ詳細: 母数を伴う条件別成績
   - 比較: 選択した馬の同じ指標による横並び
2. 「最初に見る → 比較する → 行動する」の順に並べる。
3. 実データの0件、欠損、長い馬名、18頭立て、長い記事タイトルを通常状態として設計する。
4. 同じ重要度のカードを並べて主役を曖昧にしない。カードは情報の独立性が必要な場合だけ使う。

## 色

色は役割で使い分け、同じ色に複数の意味を持たせません。

| 役割 | 色 | 用途 |
| --- | --- | --- |
| 構造 | Navy `#0f172a` | 見出し、主要な面、強い区切り |
| 操作・選択 | Blue `#2563eb` | リンク、選択中、フォーカス |
| AI分析 | Amber `#f59e0b` | AI偏差値や分析上の注目点 |
| 良好状態 | Emerald `#059669` | 更新済み、正方向の状態 |
| エラー | Red `#dc2626` | 取得失敗、入力エラー |
| 楽天競馬PR | Rose `#e11d48` | 楽天競馬のPR導線だけ |

枠色は競馬固有の識別に限って使用します。本文色は原則Slate 700以上とし、意味のある小さな文字でもSlate 500以上を使います。通常本文のコントラストは4.5:1以上を維持します。

## タイポグラフィ

- 本文は15〜16pxを基準とする。
- 操作に必要なメタ情報は11px未満にしない。装飾だけの補助表示は例外とする。
- ページ幅はホーム・レース1280px、記事一覧・データ1152px、記事紙面1080pxを上限とする。
- 記事本文はPCで1080pxの紙面内に置き、通常の見出し・段落・リストは760pxを上限とする。表・図・画像だけは1080pxまで広げ、各要素の左端を揃える。モバイルは画面端から8pxの余白を基準とし、表だけ横スクロールを許可する。
- 見出しは太さと余白で階層を作る。小さな英大文字ラベルを繰り返して階層の代わりにしない。
- 数値の桁を揃える箇所だけ等幅フォントを使う。

## 面・境界・形

- 通常の角丸は8〜12px。ピル形状はタグ、件数、状態表示に限定する。
- 枠線と広い影を同時に使わない。
- 通常コンテンツは1pxの境界線または背景差で分ける。影はsticky要素、メニュー、オーバーレイに限定する。
- モバイルで外側セクションに境界がある場合、内部の反復項目はカードを重ねず区切り線へ落とす。カード内カードによる二重の枠線を避ける。
- 広告読込中は静止した予約面を使い、継続するpulse表示でスクロール中の再描画を増やさない。広告の予約高と実広告の操作性は維持する。
- 入れ子のカードを避け、同一セクション内は罫線や余白で関係を示す。
- ガラス表現はstickyヘッダーなど、背後の文脈を保つ必要がある面だけに使う。

## 操作とモーション

- 入力直後に色、背景、境界で反応を返す。
- 色・透明度の遷移は120〜180msを基準とする。
- hoverで要素を持ち上げる、画像を拡大する、押下時に縮小する演出は使わない。
- 継続的なpulseは読み込み状態以外で使わない。
- ページ内スクロールは`prefers-reduced-motion`が有効な場合に即時移動する。
- すべての操作要素に可視フォーカスを用意し、タッチ領域は44px以上を確保する。

## ページ別ルール

### ホーム

- ヒーローは見出し、短い説明、分析の確認順、単一CTAに絞る。
- AI偏差値・対戦成績・展開/脚質・枠順傾向は、4つのミニグラフを一つの分析ガイドとしてまとめ、文章だけでは伝わりにくい比較軸を視覚化する。
- G1/Jpn1だけを強調し、その他の重賞は短い一覧にする。
- モバイルで横に隠れるレールを作らない。4件目以降は明示的に展開できるようにする。
- 記事はモバイルで1列のリスト、デスクトップで最大3列とする。
- 記事カードは32〜48字のタイトルと60〜90字の要約を基準に高さを揃え、アイキャッチがない場合もカテゴリ・日付・読了時間・見出しだけで成立させる。

### レース

- 390x844の初期表示で、AI偏差値表の先頭4頭を確認しやすい密度を基準とする。
- 開催・レース選択と予想表の内部に広告やPRを置かない。
- 予想表の列構成、枠色、欠損時の`--`表示を維持する。
- レース内ナビは「AI偏差値・対戦比較・展開／脚質・枠順傾向」の4項目に固定し、記事冒頭と同じアイコン、役割色、ミニグラフを共有する。AI展望の本文は残すが、主要4視点のナビへ混在させない。
- デスクトップのページ内ナビは4分割の平坦な区切り、モバイルの固定ボトムナビは4列を維持する。横スクロール、黒い全面選択色、説明文は使わず、選択中はBlueの罫線・淡い背景・太字を組み合わせて示す。
- モバイルの各ボトムナビ項目は44px以上、ナビ全体はsafe-areaを含めて本文へ重ならない予約余白を確保する。
- レース選択は現在の競馬場・レース要約と1〜12Rを一つのsticky面にまとめる。PCは1行、モバイルも全番号を1行に収めて横スクロールさせず、ヘッダーとの間に8px以上の余白を保つ。固定親を一つに限定し、子要素へ二重の`sticky`を付けない。
- レース選択のsticky面はヘッダーを除いて88px以内に収め、予想表側に同じレース要約を重複表示しない。
- デスクトップの右サイドバーは同日レースを重複表示せず、AI偏差値・対戦比較・展開／脚質・枠順傾向の4分析ナビを縦に配置する。
- 展開／脚質予測はPCとモバイルで同じ位置取りグラフを使う。モバイルは馬名ラベルを省略してグラフ本体を128px程度へ圧縮するが、後方・中団・先行の方向と馬番配置は維持する。枠順傾向と外枠の表示領域高を揃える。
- 4分析ボトムナビはAI偏差値表が画面へ入ってから関連記事までのレース分析範囲で表示する。Google下部アンカーが重なる時は広告DOMを変更せず、サイト側ナビを一時退避する。

### 記事

- 記事冒頭はH1 → リード → サイト価値ガイド → アイキャッチ → 目次 → 本文の順にする。カテゴリ・公開日・読了時間・著者は本文理解を遅らせるため、タイトル上へ重複表示しない。
- サイト価値ガイドはホームと同じ4つのミニグラフを使い、当日の全レース分析へのCTAを一つだけ置く。ホームは情報量のある`full`、記事冒頭は`compact`を使い分ける。
- 記事用ガイドは「今日の全レースを4つの視点で確認」と「AI偏差値・対戦比較・展開／脚質・枠順傾向」だけを表示し、説明文や独立した大型CTAを置かない。
- 記事用ガイドはモバイルでも4列を維持し、パネル全体を単一リンクにする。390px幅で全高約130px以内、各操作領域44px以上、明瞭なfocus表示を保つ。
- 記事テーマはモバイルで「重賞・騎手・コース」の3分割バーにまとめ、初期状態は44pxのバーだけを表示する。選択中テーマを押したときだけ内容を直下へ表示し、カテゴリ一覧は横スクロールさせず折り返す。閉じたバーはスクロール中もヘッダー表示高の直下へ追従する。
- 重賞テーマは二段階のアコーディオンとし、最初にG1・G2・G3・その他・Jpn1・Jpn2・Jpn3・その他だけをこの順で表示する。各重賞名は該当グレードを開いた中にだけ表示する。
- コーステーマも二段階とし、最初に競馬場名だけを表示し、その中に各距離・馬場のコースを置く。重賞・コースの階層面はSlateの単色系で統一し、グレードごとの多色塗り分けを使わない。
- 記事詳細のタイトル上には、長い記事名が省略記号だけになる前後ナビを置かない。回遊は本文後の「次に読む分析」とテーマ一覧へ集約する。
- 共通アイキャッチはOG画像と関連記事の代替表現に限定し、本文では表示しない。関連記事の代替表現にはカテゴリ別アイコンを使う。
- 目次とレース導線は紙面の補助情報として平坦に扱い、グラデーションや広い影を使わない。
- 重賞記事の対応レース導線は、開催日・レースID・正確なURL・AI偏差値データを事前検証できた場合だけ、リード直後へ表示する。上位3頭はAPI実値だけを使う。
- 対応データがない重賞記事では、ローディング枠、空のカード、「準備中」、推測した別レース、`/races/today`への代替導線を出さない。DOM自体を描画せず余白も残さない。
- 検証後の一時API障害時だけ、保存済みのレース名・日付・競馬場と正確なレースURLを残し、予測行を隠す。別レースへフォールバックしない。
- 広告とアフィリエイトの既存位置、予約高、計測を維持する。
- Googleのオファーウォールやアンカー広告が閉じた後は、表示中のGoogle UIやサイトメニューがないことを確認してから`body/html`のスクロールロックと広告由来の上下paddingを復旧する。

### データベース

- 一覧は名前、補足、母数を一行で比較できる密度にし、個別カードを並べ過ぎない。
- 詳細は通算、コース、距離、馬場、人気帯の順に置き、勝率・連対率・3着以内率には必ず出走数を併記する。
- 母数が少ない対象は`noindex`とし、検索流入向けのページ数を増やすこと自体を目的にしない。値がない箇所は推測せず`—`を表示する。
- 条件別成績の表はモバイルで横スクロールを許可し、先頭列の見出しと率の桁を揃える。
- 条件別成績と比較表は表内だけを横スクロールさせ、先頭列を固定する。固定列には行背景を引き継ぎ、率・出走数・欠損値を同時に確認できる状態を保つ。
- 保存と比較はデータ閲覧を妨げない補助操作として見出し付近へまとめ、タッチ領域44px以上と可視フォーカスを保つ。
- マイデータと比較画面は個人のブラウザ状態を使うため`noindex`とし、保存内容をサーバーへ送信しない。
- 比較画面はPCで通算・同条件・直近5走を連続表示し、モバイルでは同じ3区分を切替表示する。選択中の2〜5頭はコンパクトなsticky要約に残す。
- 比較画面では0頭、1頭、2頭、5頭、読込中、API失敗、検索0件、少数データをそれぞれ独立した状態として表示する。
- レース画面の条件別比較は初期表示で取得せず、利用者が展開した時だけAPIを呼ぶ。既存の予想表・広告・4分析の順序を崩さない。
- コースの枠番、馬番、脚質、騎手、調教師、払戻分布は同じ期間・馬場・距離条件で集計し、集計期間を画面に明示する。

### 共通ヘッダー

- 共通ヘッダーはページ最上部（8px以内）だけ表示し、本文の途中では上方向へ戻しても再表示しない。メニュー展開中は必ず表示する。
- 768px前後のタブレット幅ではモバイルメニューを維持し、主要ナビの横並びは1024px以上で表示する。PR・検索・メニューを同時に置いてもページ幅を押し広げない。
- ヘッダーに追従するsticky要素は個別の固定値を持たず、`--site-header-offset`を参照する。
- 視覚的なパンくずは回遊と現在地確認のため残し、モバイルでは「ホーム / 現在地」の1行へ圧縮する。上下6px前後の呼吸を持たせ、後続見出しへ密着させない。構造化データの`BreadcrumbList`は表示とは独立して維持する。
- モバイルメニューは`backdrop-filter`を持つヘッダーの外側へ配置し、背景を`inset: 0`、パネルをヘッダー直下から`100dvh`の残りまで固定する。
- Escape、背景タップ、フォーカストラップ、初期フォーカス、復帰フォーカスを備え、メニュー展開中は背後のスクロールと操作を止める。

## 禁止パターン

- `transition-all`
- hover時の`translate`または`scale`
- 同じ角丸・影・余白のカードを全面に敷き詰める構成
- 意味のないグラデーション、装飾目的のglass、常時アニメーション
- 押せそうに見える非操作要素
- 長文、0件、欠損値を省いた確認

## 検証

- `npm run design:audit`
- `npx tsc --noEmit`
- `npm run build`
- 375px、390px、768px、1024px、1440pxで横スクロール、sticky重なり、タッチ領域を確認する。
- 長い馬名、18頭、欠損スコア、重賞0件と多数、アイキャッチなし、長い英数字タイトル、幅広い表、広告空振りを確認する。

## 参照した外部知見

- ui-ux-pro-max: 検索可能な設計知識、アクセシビリティ、実装前チェック
- Impeccable: 共通語彙、既存デザイン優先、カード・影・モーションの抑制
- Frontend Design Discipline: 失敗条件の数値化、1変更1失敗パターン、反復検証
- Apple Design skill: 即時反応、空間的一貫性、控えめな素材、動きの中断可能性
- X記事: 主役の明確化、色の役割、実データの端ケース、削る工程

外部リポジトリのスキル本体は取り込まず、UMA-FREE固有の判断だけをこの文書とSOPへ残します。
