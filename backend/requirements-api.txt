# APIサーバー専用の最小限の依存関係
# Chrome/Selenium/pandas/numpy等の重量ライブラリを排除

# Web API & Server
fastapi
uvicorn

# Database
# 2.1 は「postgresql://」の既定ドライバが psycopg（v3）に変わるため、2.0 系に留める（2026-09-25 に API と定時処理が止まった）
sqlalchemy>=2.0,<2.1
psycopg2-binary

# Utilities
python-dotenv
